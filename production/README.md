# `team-handbook` production Docker image

## Build

```
$ make build
```

This build `harobed/team-handbook` Docker image.

## Test Production Docker image locally

```
$ make up
```

Stop it:

```
$ make down
```


## Push to Docker Registry

Push Docker image:

```
$ make push
```
